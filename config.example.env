# Video Source
# 0 for default webcam, or a file/stream path like rtsp://user:pass@ip:554/stream
SOURCE=0

# Detector: 'yolo' for YOLOv8 (requires ultralytics), 'mock' for lightweight HOG person detector
DETECTOR=yolo

# Flask
HOST=0.0.0.0
PORT=5000
DEBUG=false

# Twilio (optional). If not provided, alerts are logged to console.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM=
ALERT_TO=
